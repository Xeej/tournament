%h1.page__title
  = t('welcome.title')
%p
  .row{style: 'text-align: center;'}

%p
  = t('welcome.body3',
    log_in_link: link_to(t('welcome.log_in'), new_user_session_path, style: 'font-weight: 600;')).html_safe
